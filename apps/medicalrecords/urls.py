# from django.urls import path, include
# from rest_framework.routers import DefaultRouter
# from .views import MedicalRecordViewSet, MedicalEpisodeViewSet, PatientWithRecordsViewSet

# router = DefaultRouter()
# router.register(r'medical-records', MedicalRecordViewSet, basename='medical-records')
# router.register(r'medical-episodes', MedicalEpisodeViewSet, basename='medical-episodes')
# router.register(r'patients-with-records', PatientWithRecordsViewSet, basename='patients-with-records')

# urlpatterns = [
#     path('', include(router.urls)),
# ]
urlpatterns = [
]
